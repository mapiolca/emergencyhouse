-- Indexes and uniqueness constraints are declared inline in
-- llx_emergencyhouse.sql so repeated module activation remains idempotent.
-- Versioned migrations add future indexes only after checking information_schema.

